# LBG Alogrithm

Input
To run replace the current "myUniverse.txt" file with custom universe path or keep the same file.

Output
Shows the splitting of the codebook with the final words assigned to the classes.
Current file has first 5 'a' then next 5 'e' ... and so on.